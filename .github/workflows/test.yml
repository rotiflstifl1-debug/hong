name: Test verifier
on:
  push:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    strategy:
      matrix:
        node: [22, 24]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7, verified 2026-09-07
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7, verified 2026-09-07
        with:
          node-version: ${{ matrix.node }}
          package-manager-cache: false
      - run: node --test test/*.test.mjs
      - run: node src/cli.mjs --room technocore --file examples/published-record.jsonl --fail-on-unsigned
